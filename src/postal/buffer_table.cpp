#include "slag/postal/buffer_table.h"
#include "slag/postal/buffer.h"

namespace slag::postal {

    void BufferTable::attach_shared(BufferHandle& handle) {
        (void)handle;
        abort(); // TODO
    }

    void BufferTable::detach_shared(BufferHandle& handle) {
        (void)handle;
        abort(); // TODO
    }

    void BufferTable::attach_unique(BufferHandle& handle) {
        (void)handle;
        abort(); // TODO
    }

    void BufferTable::reattach_unique(BufferHandle& handle) {
        (void)handle;
        abort(); // TODO
    }

    void BufferTable::detach_unique(BufferHandle& handle) {
        (void)handle;
        abort(); // TODO
    }

    void BufferTable::set_shared(BufferHandle& handle) {
        (void)handle;
        abort(); // TODO
    }

}
